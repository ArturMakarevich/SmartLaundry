[build]
builder = "nixpacks"

[deploy]
startCommand = "sh -c 'python manage.py migrate --noinput && gunicorn sl_backend.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120'"
restartPolicyType = "on_failure"
