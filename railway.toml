[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn sl_backend.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
healthcheckPath = "/api/accounts/me/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
